@model IEnumerable<Library_Mvc_Jashim.Models.Book>

@{
    ViewBag.Title = "BookList";
}

<h2>BookList</h2>

<p>
    <a class="btn btn-success" href="@Url.Action("exportReport")">Download Report</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Author)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AddedTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BookCategory.Category)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Author)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AddedTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BookCategory.Category)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BookID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BookID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BookID })
        </td>*@
    </tr>
}

</table>
